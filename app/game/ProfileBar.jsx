"use client";import{useEffect,useState}from"react";import{readProfile}from"./cup";export default function ProfileBar(){const[p,setP]=useState(null);useEffect(()=>{setP(readProfile());const on=()=>setP(readProfile());window.addEventListener("ac-profile",on);return()=>window.removeEventListener("ac-profile",on)},[]);if(!p)return null;return <div className="profile-bar" aria-label="Career profile"><button type="button" className="profile-mini" onClick={()=>window.location.href="/profile"}>👤 Profile</button><span>🪙 <b>{p.coins}</b></span><span>⚽ <b>{p.matches}</b></span><span>🏆 <b>{p.wins}</b></span><span>🔥 <b>{p.streak}</b></span><span className="profile-ach">★ {p.achievements.length}</span></div>}